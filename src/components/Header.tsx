import { Box, Typography } from "@mui/material";

export const Header = () => {
  return (
    <Box>
      <Typography variant="h1" fontSize="4rem">
        Weather App
      </Typography>
    </Box>
  );
};
